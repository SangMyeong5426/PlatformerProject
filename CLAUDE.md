# 작업 규칙

이 문서는 이 저장소에서 작업하는 사람과 자동화 에이전트가 따라야 할 규칙을 정의한다.

## 이 저장소가 무엇인가

2023년에 팀으로 만든 Unity 2D 플랫포머를 **혼자 리팩토링하는 저장소**다. 목적은 게임을
더 만드는 것이 아니라 **회사 지원 시 제출할 포트폴리오**를 만드는 것이다.

그래서 이 저장소에서 가치가 있는 것은 정리된 코드 자체가 아니라 **"어떻게 검증하며
정리했는가"의 기록**이다. 코드만 깨끗해지고 과정이 남지 않으면 남는 것이 없다.

## 규칙의 출처와 이 저장소에서 달라지는 점

규칙 형식은 [`skalounge`](https://github.com/simonjiho/skalounge) 저장소를 본보기로 삼았다.
다만 그쪽은 **워크스페이스 3개 · CI · 팀 리뷰**가 있는 저장소이고 이 저장소는 **1인 · CI
없음 · PR 없음**이다. 규칙의 절반이 CI가 강제하는 장치로 되어 있어 그대로 옮기면 빈
껍데기가 된다. 그래서 다음과 같이 다르게 적용한다.

| skalounge | 이 저장소 |
| --- | --- |
| `AGENTS.md` + `CLAUDE.md` 를 CI가 동일하게 강제 | 이 파일 하나만 둔다. 도구가 하나뿐이다 |
| PR 본문이 squash 커밋이 되어 `main` 의 유일한 기록 | **PR 을 쓰지 않는다.** 그 자리를 `docs/work-log/` 가 대신한다 |
| `docs/spec/` 의 명세 사슬(화면↔API↔스키마) | 해당 없음. API 도 DB 도 없다 |
| `scripts/check-numbering` 이 ADR 번호 충돌을 검사 | 검사 없음. 브랜치가 하나뿐이라 충돌하지 않는다 |
| CI 가 도는 항목은 PR 에 적지 않는다 | **반대다. CI 가 없으므로 수동 검증을 전부 적는다** |

## 실패와 실수를 지우지 않는다

이 저장소의 가장 중요한 규칙이다. 작업 기록에 **반드시 남긴다.**

- 시도했다가 되돌린 접근과 되돌린 이유
- 작업 중 발견해서 고친 결함. 무엇이 잘못됐고, 어떻게 드러났고, 왜 그렇게 됐는지
- **검증하지 못한 항목과 그 이유**
- 오판했다가 정정한 내용. 정정했다고 해서 지우지 않는다

성공한 결과만 남은 기록은 왜 그 결과에 도달했는지 설명하지 못한다. 포트폴리오 용도에서는
이 과정 기록이 리팩토링 자체보다 차별점이 된다.

**PR 이 없으므로 이 기록은 [`docs/work-log/`](docs/work-log/) 에 남긴다.** 커밋 메시지에만
적은 것은 커밋 하나의 범위를 넘지 못한다 — "이건 해봤다가 되돌렸다"는 어느 커밋에도 속하지
않아서 커밋 메시지에는 적을 자리가 없다.

## 문서 지도

| 문서 | 무엇이 있는가 |
| --- | --- |
| [`docs/ARCHITECTURE.md`](docs/ARCHITECTURE.md) | 스크립트가 어디에 무엇이 있는가. 코드 배치와 항상 일치해야 한다 |
| [`docs/adr/`](docs/adr/) | 되돌리기 어려운 기술 결정과 그 근거 |
| [`docs/work-log/`](docs/work-log/) | 하루치 작업에서 무엇을 했고 무엇이 어긋났는가 |
| [`docs/followups.md`](docs/followups.md) | 지금은 판단할 근거가 없어 미뤄 둔 것 |
| [`docs/portfolio/`](docs/portfolio/) | 위 기록들을 면접에서 설명할 형태로 정리한 것 |

## Unity 저장소에서 특히 주의할 것

일반적인 코드 저장소와 다르게 동작하는 지점들이다. 전부 실제로 겪은 것이다.

- **`.cs` 를 옮길 때 `.meta` 를 함께 옮긴다.** 씬과 프리팹은 파일 경로가 아니라 `.meta` 안의
  GUID 로 스크립트를 참조한다. `.meta` 를 두고 오면 씬의 컴포넌트가 전부 `Missing` 이 된다.
  `git mv` 로 둘을 같이 옮기면 참조가 유지된다.
- **컴포넌트 교체는 에디터 드래그앤드롭 대신 씬 YAML 을 직접 편집한다.** 에디터로 하면 씬
  전체가 재직렬화되어 수백 줄이 바뀌고 무엇을 바꿨는지 diff 로 볼 수 없다. YAML 을 직접
  고치면 인스턴스당 3~4줄만 바뀌어 검토할 수 있다.
- **~~Unity 창이 비활성이면 플레이 모드가 진행되지 않는다.~~ 원인은 `runInBackground` 설정
  이었다.** 2026-09-01 에 정정. 증상은 맞지만 창 포커스를 원인으로 오해했다. 실제 원인은
  `ProjectSettings.asset` 의 `runInBackground: 0` 이고 `1` 로 바꿨다. 값이 0으로 나오는 것을
  코드 결함으로 오해하지 않는다는 주의는 그대로 유효하다.
- **옛 스크립트는 CP949 인코딩이다.** 편집 전에 UTF-8(BOM) 로 정규화하고, 정규화는 내용
  변경과 **별도 커밋**으로 분리한다. 섞으면 diff 가 전부 바뀌어 실제 변경을 볼 수 없다.
- **에디터가 켜져 있으면 저장하지 않은 씬 상태가 씬 전환 시 디스크에 써진다.** 의도하지 않은
  씬 변경이 작업 트리에 나타나면 되돌린다.

## 검증 규칙

CI 가 없다. 그래서 **검증은 전부 손으로 하고, 무엇을 어떻게 확인했는지 적는다.**

| 층 | 방법 |
| --- | --- |
| 컴파일 | Unity 를 refresh 하고 `Logs/` 의 `error CS` 개수를 확인 |
| 동작 동일성 | 리팩토링 전후의 동작 시퀀스를 기계 대조 |
| 런타임 | `scripts/playmode-test` (배치모드 플레이모드 테스트). 안 되면 Unity 플레이 모드에서 직접 |

**동작을 바꾸지 않는 리팩토링에서는 "컴파일이 된다"가 검증이 아니다.** 무엇이 같아야 하는지
먼저 정하고 그것을 대조한다.

**확인하지 못한 것은 확인했다고 적지 않는다.** 커밋 메시지에 무엇을 확인하지 못했는지와
어떻게 확인할 수 있는지를 적고, [`docs/followups.md`](docs/followups.md) 에 올린다.

## 기술 결정 기록

되돌리기 어렵거나 저장소 전체에 영향을 주는 선택을 **하기 전에** [`docs/adr/`](docs/adr/) 에
남긴다. 형식과 번호는 [`docs/adr/README.md`](docs/adr/README.md) 를 따른다.

- 해당한다: 코드 배치 구조, 공통 골격의 설계 방식, 저장소 운영 규칙, 마이그레이션 수행 방법
- 해당하지 않는다: 결함 수정, 문서 편집, 이미 정한 방식을 그대로 적용하는 작업

## Git 규칙

- 기준 브랜치는 `Main_Backup` 이다. 2023년 팀 작업의 마지막 상태이고 여기에 직접 커밋하지
  않는다.
- 작업은 `refactor/<name>` 등 접두사를 붙인 브랜치에서 한다.
- 접두사: `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `ci`, `perf`
- 커밋은 Conventional Commits 형식을 쓴다: `<type>(<optional-scope>): <summary>`
- 제목은 한국어 명령형으로 간결하게 쓰고 마침표를 붙이지 않는다.
- **하나의 커밋에는 하나의 논리적 변경만 넣는다.** 인코딩 정규화와 내용 변경을 섞지 않는다.
- `git add -A` 로 관련 없는 파일을 함께 stage 하지 않는다. Unity 는 에디터를 켜 두기만 해도
  `.spriteatlas`, `UserSettings/` 등을 건드린다.
- 커밋 메시지에 도구나 에이전트를 저작자로 표기하지 않는다.

## 안전 규칙

- force push, 이력 재작성, 기준 브랜치 삭제를 하지 않는다.
- `git reset --hard` 등 복구가 어려운 명령은 승인 없이 실행하지 않는다.
- 2023년 팀 작업의 커밋을 임의로 수정하지 않는다.
- 동작을 바꾸는 판단이 필요하면 **임의로 정하지 않고 묻는다.** 원래 의도가 무엇이었는지
  코드만 봐서는 알 수 없는 경우가 많다.
